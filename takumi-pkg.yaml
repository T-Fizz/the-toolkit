package:
    name: the-toolkit
    version: 0.1.0
phases:
    install:
        commands:
            - npm install
    build:
        commands:
            - npm run build
    test:
        commands:
            - npm run lint
    dev:
        commands:
            - npm run dev
